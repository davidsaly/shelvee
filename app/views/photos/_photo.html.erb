

	<%= link_to photo.picture.url , :data => { :gallery => '' }, class: "photo-links" do %>
	 <%= image_tag (photo.picture.url(:thumb)), class: "gallery-photos grid-item col3"  %>
 	<% end %>

<%#= link_to "delete", [@album, photo], method: :delete,
                                       data: { confirm: "You sure?" } %>


